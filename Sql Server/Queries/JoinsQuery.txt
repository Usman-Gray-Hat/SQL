use school


                                        **(Inner Join Query with values)**


           select a.ad_id,a.ad_name,s.std_id,s.std_name from tbl_admin a inner join student s on a.ad_id = s.ad_id_fk




                         **(Right Join , Left Join & Join query for displaying data)**


                     select * from tbl_admin a right join student s on a.ad_id = s.ad_id_fk

                     select * from tbl_admin a left join student s on a.ad_id = s.ad_id_fk

                     select * from tbl_admin a join student s on a.ad_id = s.ad_id_fk




                            **(Right Join , Left Join & Join query with values)**


       select a.ad_id,a.ad_name,s.std_name,s.std_class from tbl_admin a right join student s on a.ad_id = s.ad_id_fk

       select a.ad_id,a.ad_name,s.std_name,s.std_class from tbl_admin a left join student s on a.ad_id = s.ad_id_fk

       select a.ad_id,a.ad_name,s.std_name,s.std_class from tbl_admin a join student s on a.ad_id = s.ad_id_fk